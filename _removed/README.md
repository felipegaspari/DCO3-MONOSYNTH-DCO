# Removed unused DCO code

Files here hold code excised from the live sketch. Each file uses the **same basename** as the original source and contains **only** the removed text (plus `// --- from ... ---` markers).

Whole files that were unused (`irq_tuner.*`, `params.ino.backup_old_version`) were moved here intact.

Do not compile this folder as part of the Arduino sketch.
